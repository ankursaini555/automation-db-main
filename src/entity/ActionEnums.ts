export enum Action {
    SEARCH = "search",
    ON_SEARCH = "on_search",
    SELECT = "select",
    ON_SELECT = "on_select",
    INIT = "init",
    ON_INIT = "on_init",
    CONFIRM = "confirm",
    ON_CONFIRM = "on_confirm",
    STATUS = "status",
    ON_STATUS = "on_status",
    CANCEL = "cancel",
    ON_CANCEL = "on_cancel",
    UPDATE = "update",
    ON_UPDATE = "on_update",
    TRACK = "track",
    ON_TRACK = "on_track",
  }
  
  export enum Type {
    BAP = "BAP",
    BPP = "BPP",
  }
  
  export enum SessionType {
    AUTOMATION = "AUTOMATION",
    MANUAL = "MANUAL",
  }